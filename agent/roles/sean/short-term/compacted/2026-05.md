---
date: 2026-05
role: sean
type: compacted
last-compacted: 2026-05-27
---

# 2026-05 — Compacted Monthly Summary

## 2026-05-12

Upgraded to full ACP spec with 4-command session system (new/open/log/close session). Moved shorthand and style-guide into `agent/reference/`. Executed `collections_dir: content` reorganization — all 24 collection directories moved under `content/`, `_config.yml` updated, `docs/topic-index.md` paths fixed. Created infrastructure placeholders: `archive/`, `wiki/`, `repeatable-processes/`, `_data/track-map.yml` (three-track architecture). Created relation scan skill and config (script not yet ported). Added agentic part-writing project to todo with 5 OMR tools. Created comprehensive next-steps document. Key decision: site verification after collections_dir move added as P3 todo item. Unresolved: frontmatter enrichment not started, close session not yet tested.

## 2026-05-13

Fixed 6 ACP audit issues from hub scan: created `agent/skills/document-frontmatter.md`, expanded processing pipeline to 6 steps, removed duplicated completed-item sweep from core.md, fixed registry paths in both repos, added date computation to session protocol. Added 2 CLAUDE.md fixes (document-frontmatter in Key Files, Common Commands section). Added frontmatter backfill task at P3. Key distinction established: frontmatter backfill (P3, non-Jekyll docs) vs. frontmatter enrichment (P4, 130+ Jekyll chapter files). Marked hub registry and new/open session test complete.

## 2026-05-18

Full ACP implementation — closed all remaining gaps against the authoritative spec. Ported `relation-scan.py` from hub (203 files, 704 edges, 3-person roster). Built Miranda's infrastructure: dormant todo-handoff, identity.md with hub activation steps, role-config Hub Expansion section, `## Miranda Wilson` in todo, `workspace/shared/`. Added process tag documentation (`{process-tag}`) and close-session hook reference to core.md. Post-implementation review caught 2 gaps: added constitution-level file protection rule and role isolation rule to core.md. Compacted 3 April daily logs. First successful close session with hub snapshot export. Key decisions: Miranda's infrastructure is dormant with inline activation instructions; process tags documented but not yet used by active tasks. Unresolved: frontmatter backfill [P3] not started, high orphan count (202) expected until keyword frontmatter is added.
